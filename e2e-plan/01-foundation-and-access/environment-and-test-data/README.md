# Environment and test data

Status: configuration and setup checklist implemented; live customer, restaurant and rider login verified. Admin dashboard readiness was subsequently verified after implementing authorized profile completion in login-and-otp. Order prerequisites are checked at order execution time.

See [scenarios.md](scenarios.md) for six focused setup checks. This folder reuses LoginSmokeTest instead of adding a duplicate suite or creating test data.

## Configuration

TestConfig defaults now use customer `8000000001`, restaurant `9000000001`, rider `7000000001`. User confirmed keeping admin `1000000001`; no dedicated admin account was seeded, and the user says any number can be used. The admin default is a selected test identity, not a confirmed prepopulated profile.

Deployment selection: `-Dapp.url` overrides `E2E_APP_URL`, which overrides the central default. Phone numbers retain JVM property overrides.

## Live validation

Executed the existing four positive login cases against the configured development tunnel:

- Customer: passed, customer dashboard and role verified.
- Restaurant: passed, restaurant dashboard and role verified.
- Rider: passed using `7000000001`, rider dashboard and role verified.
- Admin: failed because the required profile-completion form appeared after OTP verification. Resolve profile completion/readability in login-and-otp; do not claim this account is dashboard-ready.

Command: `mvn -f UITesting/pom.xml '-Dtest=LoginSmokeTest#successfulLogin' -Dheadless=true -Dslow.mo=0 -Drecord.video=false -Ddefault.timeout=15000 test`

Evidence: `UITesting/target/surefire-reports/TEST-com.fooddelivery.e2e.tests.smoke.LoginSmokeTest.xml` and failure artifacts under `UITesting/target/screenshots`. Reports are regenerated by subsequent runs. Compilation succeeded; four cases ran, three passed, one failed. No profiles, addresses or orders were created.

## Source and helper references

- `src/test/java/com/fooddelivery/e2e/base/TestConfig.java`: deployment and account settings.
- `src/test/java/com/fooddelivery/e2e/base/TestBase.java`: isolated contexts.
- `src/test/java/com/fooddelivery/e2e/pages/common/LoginPage.java`: OTP interaction and setup diagnostics.
- `src/test/java/com/fooddelivery/e2e/tests/smoke/LoginSmokeTest.java`: reused readiness validation.
- Existing CustomerOutletSelectorModalPage and DeliveryOnlineTogglePage are candidates for future order preflight checks; their selectors still need live verification before reuse.

Next: login-and-otp, including admin profile handling. Recheck Home, Brand1 distance and rider availability before each order flow, not just once during this setup phase.

Update: the subsequent full login run passed all eight cases, including admin. The earlier failure recorded above is resolved; see [login results](../login-and-otp/README.md).
