# Pending work and confirmations

UI-only checkout/payment tests are not implemented in this batch. No backend fixture creation or state manipulation is allowed.

Before state-changing order cases, clarify the intended test payment flow and whether completed/cancelled test orders may remain in the shared environment. These questions do not block independent read-only or reversible UI coverage.

Reuse Home, explicitly select a nearby Brand1 outlet below 5 km, verify a rider is logged in and available nearby, and monitor customer/restaurant/rider UIs together. Do not substitute new addresses or fabricated backend state for missing prerequisites.
