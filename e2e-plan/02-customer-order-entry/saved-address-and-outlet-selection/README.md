# Saved address and outlet selection

First UI-only case validated: SavedAddressOutletUiTest passed against the deployed environment, selecting existing Home and a Brand1 outlet with displayed distance below 5 km, then checking the selected outlet in the dropdown.

See [scenarios.md](scenarios.md) and [pending work and validation notes](PENDING.md). No address or order was created. This does not verify rider availability, backend distance enforcement, or every outlet-selection scenario.
