# Saved address and outlet selection

`SavedAddressOutletUiTest` now passes four UI-only cases against the deployed environment: existing Home selection, nearby Brand1 selection with its selected checkmark, repeated address-modal dismissal while retaining Home, Home persistence on reload, and numeric nonnegative distances for every displayed Brand1 outlet option.

See [scenarios.md](scenarios.md) and [pending work and validation notes](PENDING.md). No address or order was created. This does not verify rider availability, backend distance enforcement, or every outlet-selection scenario.
