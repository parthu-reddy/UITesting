# pickup-and-delivery

Status: core pickup, delivery and reload scenarios are implemented in `HappyDeliveryFlowTest` and have completed live through exact Delivered history verification. See the cross-role lifecycle `PENDING.md` for later environment failures and the remaining final earnings assertion rerun.

Scope: Pickup, handover and delivery completion.

When starting this folder, follow the workflow in the [main plan](../../README.md). Record source/page-object references, prerequisites, scenario IDs, implementation links and validation results here. Add `scenarios.md` only at that point.

Implemented UI-only coverage includes the exact active contract, restaurant/customer address display, arrived action, pickup OTP and swipe, delivery phase and OTP, accepted-job reload, delivery-phase reload, return to Online Duty, and exact Delivered history row with restaurant and positive net payout.
